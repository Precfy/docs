{
  "openapi": "3.0.0",
  "info": {
    "title": "PDF to JSON Converter API",
    "version": "1.0.0",
    "description": "API para conversão de arquivos PDF em JSON."
  },
  "paths": {
    "/convert-pdf": {
      "post": {
        "summary": "Converte PDF em JSON",
        "description": "Recebe um PDF e retorna o conteúdo como JSON.",
        "requestBody": {
          "required": true,
          "content": {
            "multipart/form-data": {
              "schema": {
                "$ref": "#/components/schemas/PdfFile"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "JSON gerado com sucesso",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/JsonResponse"
                }
              }
            }
          },
          "400": {
            "description": "Requisição inválida"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "PdfFile": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "format": "binary",
            "description": "Arquivo PDF para conversão"
          }
        }
      },
      "JsonResponse": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string",
            "example": "success"
          },
          "data": {
            "type": "object",
            "description": "Conteúdo do PDF convertido em JSON",
            "properties": {
              "pageContent": {
                "type": "string"
              }
            }
          }
        }
      }
    }
  }
}
